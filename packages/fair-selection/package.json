{
  "name": "@bettok/fair-selection",
  "version": "0.1.0",
  "description": "Provably fair, deterministic weighted selection using SHA-256. Transparent and verifiable — every selection can be independently reproduced from its proof.",
  "license": "MIT",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
